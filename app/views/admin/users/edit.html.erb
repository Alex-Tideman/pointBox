<h1>Edit User</h1>
<%= form_for(@user, url: admin_user_path(@user)) do |f| %>
    <%= f.label :current_points %>
    <%= f.text_field :current_points %>
    <%= f.label :redeemed_points %>
    <%= f.text_field :redeemed_points %>
    <%= f.submit %>
<% end %>