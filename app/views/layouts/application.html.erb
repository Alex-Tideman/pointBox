<!DOCTYPE html>
<html>
<head>
  <title>PointBox</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header class="mdl-layout__header">
  <div class="mdl-layout__header-row">
    <!-- Title -->
    <% if current_admin? %>
        <span class="mdl-layout-title"><%= link_to current_user.username, admin_dashboard_path %></span>
        <!-- Add spacer, to align navigation to the right -->
        <div id="spacer_title" class="mdl-layout-spacer">PointBox</div>
        <!-- Navigation. We hide it in small screens. -->
        <nav class="mdl-navigation mdl-layout--large-screen-only">
          <%= link_to "Logout", logout_path, method: :delete %>
        </nav>
    <% elsif current_user %>
        <span class="mdl-layout-title"><%= link_to current_user.username, user_path(current_user) %></span>
        <!-- Add spacer, to align navigation to the right -->
        <div id="spacer_title" class="mdl-layout-spacer">PointBox</div>
        <!-- Navigation. We hide it in small screens. -->
        <nav class="mdl-navigation mdl-layout--large-screen-only">
          <%= link_to "Logout", logout_path, method: :delete %>
        </nav>
    <% else %>
        <span class="mdl-layout-title"><%= link_to "Login", login_path %></span>
        <!-- Add spacer, to align navigation to the right -->
        <div id="spacer_title" class="mdl-layout-spacer">PointBox</div>
        <!-- Navigation. We hide it in small screens. -->
        <nav class="mdl-navigation mdl-layout--large-screen-only">
          Welcome to PointBox
        </nav>
    <% end %>
  </div>
</header>
<div class="flash">
<% flash.each do |type, msg| %>
    <%= content_tag  :div, msg, class: "flash_#{type}"%>
<% end %>
</div>
<%= yield %>
</body>
</html>
